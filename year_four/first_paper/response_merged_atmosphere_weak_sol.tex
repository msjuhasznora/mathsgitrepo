\documentclass[11pt]{article}
\usepackage{graphicx}
\usepackage{amssymb}
\usepackage{bm}
\usepackage{mathtools}
\usepackage{amsmath}
\usepackage{commath}
\usepackage{amsthm}
\usepackage{enumitem}
\usepackage{float} 
\usepackage{array}
\usepackage{outlines}
\usepackage{setspace}
\usepackage{hyperref}
\usepackage[colorinlistoftodos]{todonotes}
\definecolor{green}{rgb}{0.4, 0.69, 0.2}
\definecolor{asparagus}{rgb}{0.53, 0.66, 0.42}
\definecolor{blue}{rgb}{0.0, 0.53, 0.74}
\definecolor{terracotta}{HTML}{e2725b}
\definecolor{mocassin}{HTML}{FFE4B5}
\definecolor{coral}{HTML}{FFAA80}
\setlist[enumerate,1]{label=\roman*)}
\setlist[enumerate,2]{label=\alph*)}
\newcommand*\diff{\mathop{}\!\mathrm{d}}
\usepackage{tikz}
\usetikzlibrary{arrows,shapes,trees, calc}
\usepackage{graphicx}
\usepackage{subfig}


%%% *** PREAMBLE *** %%%
\title{Answers}

\date{}

%%%***  CONTENT *** %%%
\begin{document}

\maketitle

\section{Major queries or issues}

\subsection{Motivation for non-zero vertical velocity component}

The principal reason why our approach is different from the shallow water case is that we perform asymptotical analysis. In this process it is crucial to keep every term, however small their order is. Other approaches such as using an averaged version of a function or omitting terms that are smaller than a certain order are not admissible in our case --- we investigate the asymptotic behaviour of solutions and thus we are bound to keep each term precisely.

\subsection{The matrix $\mathbf{M}$}\label{M---lambda}

\begin{enumerate}

  \item Upper bound for $\mathbf{M}$ and the coercivity $\lambda$: Generally speaking, in the scaling process we take each variable and express its possible $\epsilon$-depen-dence in an explicit way such that the remaining variable is $\epsilon$-free. For example, $z$ is the original vertical variable, in the scaling process we rewrite it as $z = \epsilon x_3,$ where now $x_3 = \mathcal{O}(1).$ Similarly, $\mathbf{K}$ is the original ("pre-scaling") diffusion matrix, and according to the (5) scaling process, the new $\mathbf{M}$ matrix is $\epsilon$-free, i.e. $M_{ij} = \mathcal{O}(1),$ simply by definition of the scaling equality. The reason why this was not clear is possibly that we did not explicitly highlight the fact that
  
\begin{gather} \nonumber
 \mathbf{M}
 =
  \begin{bmatrix}
    M_{11} & M_{12} & M_{13} \\ M_{21} & M_{22} & M_{23} \\ M_{31} & M_{32} & M_{33}
  \end{bmatrix},
\end{gather}

and not 
\begin{gather} \nonumber
  \begin{bmatrix}
    M_{11} & M_{12} & \epsilon M_{13} \\ M_{21} & M_{22} & \epsilon M_{23} \\ \epsilon M_{31} & \epsilon M_{32} & \epsilon^2 M_{33}
  \end{bmatrix},
\end{gather}

where the latter is $\mathbf{K}.$

This is now added as equation (6) in the updated file.

Considering this, $\lambda$ is naturally $\epsilon$-independent as it is a constant defined via $\mathbf{M},$ and we do not need any further boundedness assumptions throughout the proof.
  
  \item The word "homogeneous" and the identity matrix as $\mathbf{K}$: We have canceled the word homogeneous from the introduction and the general description of the phenomena since in fact we do consider a more general case. We have added a comment after equation (6) pointing out that the case of homogeneous air corresponds to
  
\begin{gather}
 \mathbf{K}
 =
  \begin{bmatrix}
    1 & 0 & 0 \\ 0 & 1 & 0 \\ 0 & 0 & \epsilon^2.
  \end{bmatrix}
\end{gather}
  
  \item The expectation that the vertical diffusivity blows up as $\epsilon \to 0$:
  \begin{itemize}
    \item Firstly, it is true that in some atmospheric models vertical viscosity is "a lot" bigger than horizontal diffusivities, but to our knowledge that is generally the consequence of specifically assuming dominant horizontal winds, and considering that in this case horizontal diffusivity effects are negligible compared to the prevailing major horizontal currents. This is certainly a possibility and an existing model in physics, but in this article we do not make this assumption, moreover even in these cases where the horizontal diffusivity constants are set to 0, the vertical diffusivity constant does not blow up as $\epsilon \to 0,$ in fact it becomes smaller and smaller as $\epsilon$ decreases as explained in the next point.
    \item Using the physical definitions for kinematic viscosity, heat diffusion and contaminant diffusion, it is generally true for these quantities that their dimensions are given by
    
    $$ [\kappa_i] = \frac{\text{length}_i^2}{\text{sec}}, $$
    
    where $\kappa_i$ is the given quantity (viscosity or diffusivity) in the $x_i$ direction, and $\text{length}_i$ is the length scale in the $x_i$ direction. This also suggests that vertical diffusivity is small ($\mathcal{O}(\epsilon^2)$) when the length scale in the vertical direction is small ($\mathcal{O}(\epsilon)$). We have updated the original $\text{typical length}^2 / \text{typical time}$ explanation to $[\nu_{x_i}] = \text{typical length}_i^2 / \text{typical time}$ to support the correctness of the scaling.
    \item To answer this suggestion from another viewpoint, let's consider the following experiment. Let's take a shallow aquarium of 1 meter x 1 meter x 1 cm. The aquarium is closed from all sides, in the beginning it only contains clean air, and at some point we place a high concentration of pollens in the middle of the domain. The physical reality is that pollens diffuse in horizontal directions, and they do not stay concentrated along the $z$ axis (while vertical diffusivity blowing up for shallow domains would mean that the diffusion of pollens takes place most dominantly along $z,$ and almost no pollen diffusion happens towards the horizontal directions).
    \item Finally, using the analogy between large-scale ocean models and large-scale atmosphere models, it is reasonable to expect that diffusion effects in the calm fluid take place in similar orders. In the finite element Northern Atlantic ocean model of \cite{danilov2004finite} horizontal diffusivity is set to 200 $m^2/s,$ while vertical diffusivity is $0.0002$ $m^2 /s.$ Observed values for horizontal and vertical diffusivity in \cite{talley2011descriptive} are 2 $m^2 / s$ and $10^{-4}$ $m^2/s$, respectively. Thus, empirical results suggest that vertical diffusivity in shallow domains is by several orders smaller than horizontal diffusivity.
    
  \end{itemize}
  
  \item The boundedness of the matrix $\mathbf{M}$ with respect to $\epsilon$: as previously described, $M_{ij} = \mathcal{O}(1),$ and we do not use any further assumptions.
  
  \item The idea of denoting $\mathbf{M}$ by $\mathbf{M}^\epsilon:$ the functions such as $\mathbf{u}^\epsilon$ have an $\epsilon$ in their notation because they are solutions of an equation containing an $\epsilon$ parameter. However, the constants $\nu_i$ and $\mathbf{M}$ do not depend on $\epsilon$, and thus, even though they are used in the anisotropic framework, in the case of these parameters we do not use the $\epsilon$ index in their notation.

\end{enumerate}

\subsection{Regularised Dirac masses}

\begin{enumerate}

  \item Why we regularise the source in the anisotropic system: when we derive the energy inequality (33), the $\int_0^t (S^\epsilon, C^\epsilon) d \tau$ term needs to be bounded, and for this we use the $L^\infty(\Omega)$ regularity of $S^\epsilon.$ The proof of our theoretical result has stronger requirements of the anisotropic source term than it does of the hydrostatic pollution source.
  
  \item Why the regularisation parameter $\kappa$ is isotropic: because at this point the scaling process is already complete. We have passed from the original $Q$ source term to $S^\epsilon$ in (8). Once the scaling process is done, we start by giving a concrete form to the source function on $\Omega,$ and we choose to use Gaussians with regularisation parameter $\kappa$ for this purpose. We could make the decision to keep two small parameters that tend to zero in the model: $\epsilon$ representing shallowness, while $\kappa$ representing a regularisation parameter in the Gaussians. Keeping these two different parameters does not give any additional value to the theoretical results, for simplicity we set $\kappa$ to $\epsilon.$

\end{enumerate}

\section{Minor queries}

\subsection{Cross references}
Further references have been added.

\subsection{p. 6-7 discussion around pollution source}

We have chosen to use the "approximated" terminology and throughout the paper we have replaced the term "nascent" with this. Furthermore, we have shortened the description of the pollution source by a few words, however most of it feels necessary to us.

\subsection{Definition 1 and 2}

These definitions have been updated.

\section{Typos, Grammar}

\subsection{Extra space following each displayed equation}

The additional empty lines have been eliminated.

\section{Further comments}

\subsection{Page 4}
Number: 2. To our knowledge, "analogues" is a noun in plural form, while "analogous" is an adjective. Since in our text we use this word as "air-analogous version", hence as an adjective, we feel that the "ou" spelling is the one we need. (e.g. \href{https://www.merriam-webster.com/dictionary/analogue}{https://www.merriam-webster.com/dictionary/analogue})

\subsection{Page 6}

\begin{enumerate}
  \item Number: 1. As it is described in page 5, "$x$,$y$ and $z$ are oriented towards east, north, and upwards, respectively," and in this case the $\mathbf{w}$ term takes the orientation $\mathbf{w} = f \cdot (0, \cos(l(y)), \sin(l(y))),$ where $l$ is the latitude, and $f$ is the module (since this is described in \cite{azerad2001mathematical}, we feel it is unnecessary to detail this information in our paper). For simplicity we denote $2 \mathbf{w} = (0, \beta, \alpha).$ As for the question regarding whether the Earth rotation angular speed makes sense in our approach, we highlight that we do not "flatten out" the Earth: it is still described as a rotating globe. We focus our attention on a local area on the surface of the rotating globe, and we approximate this surface with a tangent plane. This plane is still assumed to be rotating, hence we do have the effect of the Coriolis force in our model.
  \item Number: 2. Our system and our theoretical result are principally set in the framework of the atmosphere and pollutants, but we aim to leave space for generalisations and further natural transplantations of the model. For example, beyond the example of the atmosphere, the mathematical result and system itself can be used to describe phenomenon such as sand mixing in the ocean, and possibly other physical processes that right now are unpredictable for us. The diffusion matrix can be calibrated as necessary considering the concrete application, our results are valid whether we use a diagonal matrix or not, we never excluded the possibility of the non-diagonal terms being zero. We simply chose to work with the more general case, allowing these terms to be either zero or non-zero depending on the concrete case the model is applied to.
\end{enumerate}

\subsection{Page 7}

\begin{enumerate}

  \item Number: 3. $\lambda$ is independent of $\epsilon.$ This issue is explained in detail Section \ref{M---lambda} of this document.
  \item Number: 4. We have decided to avoid concentration definitions that include technical terms from chemistry unnecessarily overshadowing mathematical clarity, thus we simply use mass / volume dimensions to grasp concentration. Conversion rules to ppmv units can be found for example in \href{https://en.wikipedia.org/wiki/Air\_pollutant\_concentrations}{https://en.wikipedia.org/wiki/Air\_pollutant\_concentrations}.
  \item Number: 8. We mean that $\kappa$ is constant with respect to $\mathbf{x}$ and $t$ (in the case of $S^\epsilon,$ for a given $\epsilon$ value).
  \item Number: 11. As the word "smoke" might suggest that we consider particles from a source of burning, we use the word "contaminant".
  
\end{enumerate}

\subsection{Page 8}

\begin{enumerate}

\item Number: 8. As detailed at a later point in the article, in Remark 6 specifically, there are several good choices to give concrete shape to the source function. We chose one to give an example, but as long as the requirements of smoothness and physical meaningfulness are satisfied, several other options are possible.
\item Number: 9. The constant $\gamma$ is indeed $\pi ^ {3/2}.$ Since the exact numerical value of this constant does not play an important role in the calculations, we omit its value for simplicity.

\end{enumerate}

\subsection{Page 10}

Number: 2. What we mean here is in case we have $C^\epsilon(x,y,z) = c$ on the boundaries, then we can take this $c$ value, make a change of variables $C^\epsilon_\text{new} = C^\epsilon_\text{original} - c$ on $\Omega,$ obtaining a new $C^\epsilon$ concentration function that is constant $0$ on the boundary. The original concentration function has this additional "background flow" of contaminants of constant concentration with respect to the new function obtained after the change of variables.

\subsection{Page 12}

\begin{enumerate}

\item Number: 1. $\Gamma_A$ is the part of the $\partial \Omega = \Gamma$ boundary that is above the ground ("A" stands for "above"). We use this notation ($\Gamma_A$ in the index) to emphasise that the functions in this space are required to vanish only on this given part of the entire $\Gamma$ boundary.

\item Number: 2. The missing "A" has been added. The definition has been updated with the appropriate vanishing $\partial^\alpha v = 0$ requirements. We do need this definition for $k=2$ in Definition 1 and Definition 2 for the $\tilde{C}$ test function.

\end{enumerate}

\subsection{Page 13}

Number: 1. As the paper has a section called Notations in the beginning dedicated only to clarify certain notations, and since the definition of $b(\mathbf{u}_H) = \alpha (-u_2, u_1)$ is provided there under Point \textit{n)}, we feel like it is somewhat redundant to mention more about it.

\subsection{Page 15}

\begin{enumerate}

\item Number: 1. The nonlinear terms fall out because of the divergence-free condition and integrating by parts using the boundary conditions. For example, using integration by parts and exploiting that we do not have boundary terms as $\mathbf{u} = 0$ on the whole $\partial \Omega = \Gamma$ boundary, the $(\mathbf{u} \cdot \nabla u_1, u_1)$ inner product is equal to $ - \int_\Omega (\nabla \cdot \mathbf{u}) \cdot u_1^2 - \int_\Omega \mathbf{u} u_1 \nabla u_1.$ Hence, using div($\mathbf{u}$) = 0, we have

$$ \int_\Omega u_j \partial_j u_1 \cdot u_1 = - \int_\Omega u_j u_1 \partial_j u_1,$$

thus these nonlinear terms are zero.

On the other hand, the $b(\mathbf{u}_h) \cdot \mathbf{u}_H$ term vanishes as a direct consequence of the definition of $b$ (Notations / \textit{n)}), as 

$$b(\mathbf{u}_h) \cdot \mathbf{u}_H = \alpha (-u_2, u_1) \cdot (u_1, u_2) = \alpha \cdot 0 = 0.$$

\item Number: 2. For better readability we prefer to keep the larger bracket.

\end{enumerate}

\subsection{Page 17}

Number: 1. The time shift parameter $h$ is a non-negative number here (we take the norm $\norm{\tau_h \mathbf{u}_H - \mathbf{u}_H}$ in the $L^p(0, T-h, \mathbf{Y})$ space, thus, specifically, on the time interval $[0, T-h]$). The respective criteria for a negative $h$ value would be $\norm{\tau_h \mathbf{u}_H - \mathbf{u}_H}_{L^p(h, T, \mathbf{Y})} \leq \varphi(h) + \psi(\epsilon).$ For clarity we have noted that $h$ is non-negative in our description.

\nocite{*}
\bibliographystyle{abbrv}
\begin{thebibliography}{1}

\bibitem{danilov2004finite}
S.~Danilov, G.~Kivman, and J.~Schr{\"o}ter.
\newblock A finite-element ocean model: principles and evaluation.
\newblock {\em Ocean Modelling}, 6(2):125--150, 2004.

\bibitem{talley2011descriptive}
L.~D. Talley.
\newblock {\em Descriptive physical oceanography: an introduction}.
\newblock Academic press, 2011.

\bibitem{azerad2001mathematical}
P.~Az{\'e}rad and F.~Guill{\'e}n.
\newblock Mathematical justification of the hydrostatic approximation in the
  primitive equations of geophysical fluid dynamics.
\newblock {\em SIAM Journal on Mathematical Analysis}, 33(4):847--859, 2001.

\end{thebibliography}

\end{document}

