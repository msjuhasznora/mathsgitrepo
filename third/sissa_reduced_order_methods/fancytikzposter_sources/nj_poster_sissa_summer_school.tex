% fancytikzposter.tex, version 2.1
% Original template created by Elena Botoeva [botoeva@inf.unibz.it], June 2012
% 
% This file is distributed under the Creative Commons Attribution-NonCommercial 2.0
% Generic (CC BY-NC 2.0) license
% http://creativecommons.org/licenses/by-nc/2.0/ 

% change sizes: \tiny 12pt \scriptsize 14.4pt \footnotesize 17.28pt \small 20.74pt \normalsize 24.88pt
% http://ctan.mirror.garr.it/mirrors/CTAN/macros/latex/contrib/a0poster/a0_eng.pdf

\documentclass{a0poster}

\usepackage{fancytikzposter} 
\newcommand*\diff{\mathop{}\!\mathrm{d}}
\setcolumnnumber{2}

\definecolor{mygreen}{HTML}{00CC88}
\definecolor{mygrey}{HTML}{D3D3D3}
\definecolor{mypink}{HTML}{FFAA80}
\setfirstcolor{mygrey}
\setsecondcolor{mypink}
\setthirdcolor{mygreen}

%%% size of the document and the margins
%% A0
% \usepackage[margin=\margin cm, paperwidth=118.9cm, paperheight=84.1cm]{geometry} 
\usepackage[margin=\margin cm, paperwidth=84.1cm, paperheight=118.9cm]{geometry}

\usepackage{cmbright}
\usepackage{amsmath}
\usepackage{commath}
\usepackage{amsthm}
\usepackage[math]{kurier}
\usepackage[T1]{fontenc}

\title{The polluted atmosphere as a shallow domain}
\author{Nora Juhasz\\
  DISIM, University of L'Aquila, Italy\\
  \texttt{ms.juhasz.nora@gmail.com}\\
  joint work with D. Donatelli, University of L'Aquila
}

\begin{document}

\ClearShipoutPicture
\AddToShipoutPicture{\BackgroundPicture}

\noindent % to have the picture right in the center
\begin{tikzpicture}
  \initializesizeandshifts

  \ifthenelse{\equal{\template}{1}}{ 
    \titleblock{47}{1}
  }{
    \titleblock{47}{1.5}
  }

  \addlogo[south west]{(1,1)}{7cm}{ModCompShock.jpg}
  \addlogo[south east]{(-1,1)}{4.5cm}{LogoDisim.png}

  %% a block node, with the specified position (optional), title and the content
  %% #1 - where (optional), #2 - title, #3 - text
  %%%%%%%%%% ------------------------------------------ %%%%%%%%%%
  \blocknode{\textcolor{gray}{The shallow domain concept}}
  {
  \begin{itemize}
    \item Natural, large-scale geophysical fluids share the shallow domain characteristics
    \item "almost" two-dimensional set $\to$ we can significantly simplify the model in the vertical direction
    \item a domain that is excessively greater horizontally than in the vertical dimension $\to$ leading to the classic hydrostatic approximation of the Navier-Stokes equations
  \end{itemize}
  
  \vspace{0.5cm}
  
  The main ideas and tools in this scenario:
  \begin{itemize}
    \item introduce the aspect ratio $\epsilon = \frac{\text{characteristic depth}}{\text{characteristic width}},$
    \item anisotropic viscosity hypothesis,
    \item rescaling, $x=x_1, y=x_2, z=\epsilon x_3.$
  \end{itemize}
  
  }

  %% a callout block
  %% #1 - rotate angle (optional), #2 - from, #3 - where, #4 - width, #5 - text
  %%%%%%%%%% ------------------------------------------ %%%%%%%%%%
  \calloutblock
  {($(box.center)+(-2,-7)$)} %% the point of the arrow part of the block
  {($(box.center)+(12,-3)$)} %% the center of the body of the block
  {15cm} %% the width of the block
  {\small
  Correspondingly, we rescale
  
  \begin{itemize}
    \item the kinematic terms: $v_3 = \epsilon u_3^{\epsilon},$
    \item the viscosity terms: $\nu_z = \epsilon ^ 2\nu_3,$
    \item the diffusivity terms: $K_x =  K_1, K_y = K_2, K_z = \epsilon ^ 2 K_3.$
  \end{itemize}
  }
  
    %%%%%%%%%% ------------------------------------------ %%%%%%%%%%
  \blocknode{\textcolor{gray}{Modelling choices}}
  {
  \begin{itemize}
    \item Coordinate system $\to$ we use a classical geophysical coordinate choice: north -- east -- upwards directions.
     
    \item Domain $\to$ we use locally acceptable Cartesian coordinates --- this allows us to ignore large-scale effects such as the curvature of the Earth.
     
      \begin{tikzfigure}[A local slice of the atmosphere]
        \begin{tikzpicture}
      
          \node[inner sep=0pt] (domain) at (0,0) {\includegraphics[width=.2\textwidth]{domain.jpg}};
      
        \end{tikzpicture}
      \end{tikzfigure}
  
    Challenges: local area model $\to$ open boundary conditions on the lateral boundary. Periodicity in this case is quite unrealistic. We use a fix zero approach --- the concentration being set to zero actually stands for a constant concentration with the background flow being removed.
    
  \end{itemize}
  }

  %%%%%%%%%% ------------------------------------------ %%%%%%%%%%
  \blocknode{\textcolor{gray}{The anisotropic equations}}
  { 
    \coloredbox{colorthree!50!}{
      
    \[ \partial_t u^\epsilon _1 + \bold{u}^\epsilon \cdot \nabla u^\epsilon _1 -\Delta_\nu u^\epsilon _1 -\alpha u_2^\epsilon + \epsilon \beta u_3^\epsilon + \partial_1 p^\epsilon = 0 \text{ in } \Omega \times (0,T) \]
    
    \[ \partial_t u_2^\epsilon + \bold{u}^\epsilon \cdot \nabla u_2^\epsilon -\Delta_\nu u_2^\epsilon + \alpha u_1^\epsilon + \partial_2 p^\epsilon = 0 \text{ in } \Omega \times (0,T) \]
    
    \[\epsilon^2 \{  \partial_t u_3^\epsilon + \bold{u}^\epsilon \cdot \nabla u_3^\epsilon -\Delta_\nu u_3^{\epsilon} \} - \epsilon \beta u_1^\epsilon + \partial_3 p^\epsilon = 0 \text{ in } \Omega \times (0,T) \]
    
    \[ \nabla \cdot \bold{u}^\epsilon= 0 \text{ in } \Omega \times (0,T) \]
    
    \[ \partial_t C^\epsilon + \bold{u}^\epsilon \cdot \nabla C^\epsilon = K_1 \frac{\partial ^2 C^\epsilon}{\partial x_1 ^ 2} + K_2 \frac{\partial ^2 C^\epsilon}{\partial x_2 ^ 2} + K_3 \frac{\partial ^2 C^\epsilon}{\partial x_3 ^ 2} + S_\epsilon \text{ in } \Omega \times (0,T) \]
    
    \[ \bold{u}^\epsilon (\cdot, t = 0) = \bold{u}_0, C^\epsilon (\cdot, t=0) = C^\epsilon_0  \text{ in } \Omega \]
        
    }
  }
  
  \plainblock[5]{($(currenty)+(1,3)$)}{25}{The original equations} 
  {
    \vspace{0.3cm}
    We use the
    
    incompressible Navier-Stokes equations in all three space dimensions for the fluid velocity.
  }

  \blocknode[($(currenty)-(0,4.8)$)]{\textcolor{gray}{The limit model}}
  { \small In the vertical dimension we arrive to the hydrostatic approximation.
    \vspace{0.5cm}
    \coloredbox{colorthree!50!}{
    
    \[ \partial_t u_1 + \bold{u} \cdot \nabla u_1 - \Delta_\nu u_1 -\alpha u_2 + \partial_1 p = 0 \text{ in } \Omega \times (0,T) \]
    
    \[ \partial_t u_2 + \bold{u} \cdot \nabla u_2 - \Delta_\nu u_2 -\alpha u_1 + \partial_2 p = 0 \text{ in } \Omega \times (0,T) \]
    
    \[ \partial_3 p = 0 \text{ in } \Omega \times (0,T) \]
    
    \[ \nabla \cdot \bold{u} = 0 \text{ in } \Omega \times (0,T) \]
    
    \[ \partial_t C + \bold{u} \cdot \nabla C = K_2 \frac{\partial ^2 C}{\partial x_2 ^ 2} + K_3 \frac{\partial ^2 C}{\partial x_3 ^ 2} + S \text{ in } \Omega \times (0,T) \]
    
    \[ u_1 = u_2 = u_3 n_3 = 0 \text{ on } \Gamma \times (0,T) \]
    
    \[ u_i (\cdot, t=0) = u_{0 i}, C (\cdot, t=0) = C_0 \text{ in } \Omega, \text{$i$ = 1,2} \]
    
    \[ C = 0 \text{ on } ( \Gamma_U \cup \Gamma_L) \times (0,T), \partial_1 C, \partial_2 C \in L^2 L^2 (0,T; \Gamma_G), \partial_3 C = 0 \text{ on } \Gamma_G \times (0,T) \]
    }
  }
  
    %%%%%%%%%%%%% NEW COLUMN %%%%%%%%%%%%%%% 
  \startsecondcolumn 
    %%%%%%%%%% ------------------------------------------ %%%%%%%%%%
    
  
  %%%%%%%%%% ------------------------------------------ %%%%%%%%%%
  \blocknode{\textcolor{gray}{The main theorem}}%
  {
      
    \begin{tabular}[t]{ll}
      \begin{minipage}{1.0\linewidth}
        \innerblock{Theorem} {Let $\bold{u}_0$ $\in$ $L^2 (\Omega)^3$, with $\nabla \cdot \bold{u}_0 = 0, \bold{u}_0 \cdot \bold{n} = 0$ on $\partial \Omega$; and let $C(0)\in$ $L^2 (\Omega)^3$, $C(0) = 0$ on $ \partial \Omega$; there exists a weak solution ($\mathbf{u}$, C) of the hydrostatic equations of the polluted atmosphere, obtained as a limit of weak solutions $(\mathbf{u}^\epsilon, C^\epsilon)$ of the anisotropic equations as the aspect ratio $\epsilon$ tends to $0$.}
      \end{minipage}
    \end{tabular}
 
  }
  
  %%%%%%%%%% ------------------------------------------ %%%%%%%%%%
  \blocknode {\textcolor{gray}{A priori estimates, weak convergence}}
  {
  
    \small The main steps for proving the weak convergence are the following.
    \begin{itemize}
      \item Energy inequality.
    \end{itemize}
      
    \coloredbox{colorthree!50!}{
    
    \[
    \begin{split}  
    & \frac{1}{2} (\norm{\mathbf{u}_H^\epsilon (t)}_{L^2}^2 + \epsilon ^ 2 \norm{u_3^\epsilon (t)}_{L^2}^2 + \norm{C^\epsilon (t)}_{L^2}^2) + \int_0 ^ t \big [\norm{\nabla_\nu \mathbf{u}_H^\epsilon}_{L^2}^2 + \epsilon ^ 2 \norm{\nabla_\nu u_3^\epsilon}_{L^2}^2 \big ] \diff \tau \\
    & + \int_0 ^ t \bigg[ (\lambda - \sum_{i,j = 1}^3 \zeta \frac{1}{2} M_{ij} c_T) \norm{\nabla C^\epsilon}_{L^2}^2  \bigg] \diff \tau \\
    & \leq \sum_{i,j = 1}^3 \frac{1}{\zeta} M_{ij} \frac{1}{2} \int_0 ^ t \norm{\sigma_i}^2 _{L^2 (\Gamma_G)} \diff \tau + \frac{1}{2} (\norm{\mathbf{u}_H^\epsilon (0)}_{L^2}^2 + \epsilon ^ 2 \norm{u_3^\epsilon (0)}_{L^2}^2 + \norm{C^\epsilon (0)}_{L^2}^2) \\
    & + \int_0^t (S^\epsilon,C^\epsilon) \diff \tau + \sum_{i,j = 1}^3 \zeta \frac{1}{2} M_{ij} c_T  \int_0 ^ t \norm{C^\epsilon}^2 _{L^2 (\Omega) } \diff \tau.
    \end{split}
    \]
    
    }
      
    \begin{itemize}  
      \item A priori estimates: $L_t^\infty L_x^2$ boundedness for $u_1^\epsilon, u_2^\epsilon, \epsilon u_3^\epsilon, C^\epsilon;$ $L_t^2 H_x^1$ boundedness for $u_1^\epsilon, u_2^\epsilon, \epsilon u_3^\epsilon, C^\epsilon.$
      \item Weak convergence of the linear terms.
      \item Weak convergence of nonlinear terms.
      \begin{itemize}
        \item Nonlinear terms not including the $C^\epsilon$ term - divergence free condition
        \item The term $(\mathbf{u}^\epsilon C^\epsilon, \nabla \tilde{C}).$
      \end{itemize}
    \end{itemize}
  }
  
      %%%%%%%%%% ------------------------------------------ %%%%%%%%%%
  \calloutblock
  {($(box.center)+(-11,-11.5)$)} %% the point of the arrow part of the block
  {($(box.center)+(2,-14.5)$)} %% the center of the body of the block
  {33cm}
  {
    We need a compactness result to obtain $u_3^\epsilon C^\epsilon \rightharpoonup  u_3 C.$
    
    For this we use that
    
    \begin{itemize}
    
      \item $u_3^\epsilon$ is weakly convergent,
      
      \item we show that $C^\epsilon$ is strongly convergent in space-time in $L^2_t L^2_x$. We prove this by using a variant of the Aubin-Lions lemma.

    \end{itemize}
    
    The combination of these results provides the weak convergence of the product.
  }
  
  \getcurrentrow{note}
  \coordinate (currenty) at ($(currentrow)+(xshift)-(yshift)$);
  
  \blocknode{\textcolor{gray}{Numerical symulations - \textbf{a challenge}}}
  {
    \begin{itemize}
      \item In order to visualise the convergence result we use the finite element method
      \item Challenge: the FEM matrix is more and more ill-conditioned as the weight of $u^\epsilon_3$ is disappearing from the momentum equation
      
      \item CODE
      \item CODE
      \item CODE
      \item CODE
      \item CODE
      \item CODE
      \item CODE
      \item CODE
      \item CODE
      \item CODE
      \item CODE
      \item CODE
      \item CODE
      \item CODE
      \item CODE
      
    \end{itemize}
  }
 
  \blocknode{\textcolor{gray}{Additional results}}
  {
    \begin{itemize}
      \item We also provide an existence theorem for the extended system.
      \item We have upgraded the model for the case of a downwind-matching coordinate system. In this scenario the $x$ axis is oriented towards the downwind / main wind direction, and leads to the appearance of an additional $\epsilon$ multiplier in the concentration equation --- in the $x$ direction diffusion is negligible compared to wind transport.
    \end{itemize}
  }

\end{tikzpicture}

%\nocite{*}
%\bibliographystyle{apalike}
%\bibliography{nj_poster_vertical}
\end{document}